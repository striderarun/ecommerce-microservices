CREATE TABLE CUSTOMER
(
ID INTEGER,
NAME VARCHAR(20),
EMAIL VARCHAR(20),
PHONE_NO VARCHAR(20),
PRIMARY KEY (ID)
);

CREATE TABLE CUSTOMER_ORDERS
(
ID INTEGER,
CUSTOMER_ID INTEGER,
ORDER_ID INTEGER,
ORDER_STATUS VARCHAR(10),
PRIMARY KEY (ID)
);


