CREATE TABLE ORDERS
(
ID INTEGER,
CUSTOMER_ID INTEGER,
COST INTEGER,
STATUS VARCHAR(20),
PRIMARY KEY (ID)
);

CREATE TABLE ORDER_DETAILS
(
ID INTEGER,
ORDER_ID INTEGER,
PRODUCT_ID INTEGER,
PRIMARY KEY (ID),
FOREIGN KEY (ORDER_ID) REFERENCES ORDERS (ID)
);


